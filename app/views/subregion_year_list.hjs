<h3 class="h5 mb-3">{{subRegionName}} ({{year}})</h3>

{{#hasData}}
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Country</th>
        <th class="text-end">Internet Users</th>
      </tr>
    </thead>
    <tbody>
      {{#rows}}
        <tr>
          <td>{{CountryName}}</td>
          <td class="text-end">{{InternetUsers}}</td>
        </tr>
      {{/rows}}
    </tbody>
  </table>
{{/hasData}}

{{^hasData}}
  <div class="alert alert-warning mb-0">
    No data found for this sub-region in {{year}}.
  </div>
{{/hasData}}
